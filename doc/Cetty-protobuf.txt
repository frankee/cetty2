client:

service_stub(channel);

service_stub->serviceMethod()
				channel->write(rpcEvent)
							RpcMessageHandler->HandleDownstream()
							
							
Server:

RpcMessageHandler->OnReceivedMessage(rpcEvent);
	